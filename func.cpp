#include "func.h"

using namespace std;

RES dvd_subcad(CADENA cad, int inicio, int final){
	int caso_base, mitad;

	//caso base
	caso_base = cad.getSize_Subcad();
	if( .... <= cad.get)
		cad.comprobar_Subcad(inicio, final);
	else{
		int mitad = 
		dvd_subcad(...);
		dvd_subcad(....);
		dvd_combinar(.....);
	}
}